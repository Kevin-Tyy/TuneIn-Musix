interface UserType {
    username : string;
    email : string;
    authenticationmethod : "email_password" | "oauth"
    savedMusic : Array<any>;
    avatarUrl : string;
}
interface MusicType {
    
}